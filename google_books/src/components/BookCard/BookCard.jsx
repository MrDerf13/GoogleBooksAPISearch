const BookCard = ({ image, author, title, description }) => {
  return (
    <div>
      {/* image */}
      {/* title */}
      {/* author */}
      {/* description - preview with 10 or so words then have a button to click to look at the description on the blurb */}
      {/* ASPIRATIONAL - button in bottom right to animate the book flipping over and having the full description on the back*/}
    </div>
  );
};

export default BookCard;
